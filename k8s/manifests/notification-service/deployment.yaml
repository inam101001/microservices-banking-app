apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: microservices
  labels:
    app: notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: inam101001/notification-service:dev
          imagePullPolicy: Always
          ports:
            - containerPort: 8004
          env:
            - name: NOTIFICATION_SERVICE_DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: notification-service-config
                  key: NOTIFICATION_SERVICE_DATABASE_URL
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 8004
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 5
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.28
          command: ['sh', '-c', "until nc -z notification-postgres 5432; do echo waiting for postgres; sleep 2; done"]